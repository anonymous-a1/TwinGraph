# TwinGraph: A Dynamic Joint Graph Structure for Approximate Nearest Neighbor Search

This is the source code of TwinGraph

## Linux build


### Prerequisites
 * Ubuntu 22.04.0 LTS
 * GCC >= 11.4.0
 * Boost >= 1.71

### Build 

```
cd TwinGraph/tests
mkdir build && cd build
cmake .. && make -j
```
